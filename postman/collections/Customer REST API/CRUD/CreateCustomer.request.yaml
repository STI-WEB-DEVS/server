$kind: http-request
name: CreateCustomer
url: http://localhost:8000/api/customers
method: POST
auth:
  credentials:
    - key: token
      value: "{{token}}"
  type: bearer
settings:
  disabledSystemHeaders:
    - host
    - user-agent
    - accept-encoding
    - connection
order: 1000
